# このプロジェクトのルール（CLAUDE.md）

このファイルは、Claude Code がこのプロジェクトで作業する際に必ず守るべきルールをまとめたものです。

---

## 対象者

このプロジェクトはプログラミング初心者が開発を行っています。
そのため、以下のルールを厳守してください。

---

## ルール1：コード変更・作成前に必ず意図を説明する

- ファイルを新規作成・編集・削除する **前に**、「何をどう変えるのか」「なぜそうするのか」を日本語でわかりやすく説明すること。
- 専門用語を使う場合は、必ず補足説明を添えること。

## ルール2：既存ファイルの上書きや外部アクセスは必ず事前確認する

- すでに存在するファイルを書き換える場合は、実行前にユーザーへ確認を求めること。
- インターネットへのアクセス（API呼び出し、スクレイピング等）を行う前も、必ず確認を求めること。
- ユーザーが「OK」「はい」「進めて」などの明示的な許可を出すまで、実行してはならない。

## ルール3：MVP（最小限の機能）を最優先にする

- 複雑な機能・抽象化・将来の拡張を考えた設計は**禁止**。
- 「今、動くこと」だけを目的とした、最もシンプルなコードを書くこと。
- 1ファイル・1機能の原則を守ること。

---

## プロジェクト概要

ポケモンカードのせどり（転売）を補助するための Discord 自動通知 BOT を開発する。

- メインファイル: `simple_bot.py`
- 使用ライブラリ: `requests`, `beautifulsoup4`
- 通知先: Discord Webhook

---

## 開発ステップ

1. [x] CLAUDE.md の作成（初期ルール設定）
2. [ ] ユーザーが Discord Webhook URL を取得する
3. [ ] `simple_bot.py` の作成（ユーザーの OK 後に着手）
